const {
  errorHandler
}                         = require('./error-handler');
const {
  createQueryTop5Ratings
}                         = require('./tours');

module.exports = {
  errorHandler,
  createQueryTop5Ratings
}