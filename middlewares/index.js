const {
  errorHandler
}                         = require('./error');
const {
  createQueryTop5Ratings
}                         = require('./tours');

module.exports = {
  errorHandler,
  createQueryTop5Ratings
}