const {
  errorController
}                         = require('./error');
const {
  createQueryTop5Ratings
}                         = require('./tours');

module.exports = {
  errorController,
  createQueryTop5Ratings
}